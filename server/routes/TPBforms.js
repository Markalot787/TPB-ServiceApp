// server/routes/TPBforms.js

router.get('/preview/:formId', async (req, res) => {
	// Fetch form data by formId and send it to the client
});

router.post('/submit/:formId', async (req, res) => {
	// Save submitted form data to your server
});
